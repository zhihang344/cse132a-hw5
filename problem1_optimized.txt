-- Problem 1: Optimized logical plan in relational algebra goes here.


pi_{dname, name, pid} (
    (
        (
            pi_{deptid, dname} (sigma_{location = 'LA'} (Department))
            bowtie_{deptid} 
            pi_{pid, deptid} (sigma_{budget >= 100000 AND budget <= 300000} (Project))
        )
        bowtie_{pid} 
        pi_{empid, pid} (Assignment)
    )
    bowtie_{empid} 
    pi_{empid, name} (sigma_{salary > 80000} (Employee))
)